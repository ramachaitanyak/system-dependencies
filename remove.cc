#include "remove.h"

void
Remove::remove_sw(std::string sw, std::map<std::string, std::vector<std::string>> used_by_map,
        std::vector<std::string>) {
    // Remove sw if it is not in used_by_map and currently installed
    //

}
